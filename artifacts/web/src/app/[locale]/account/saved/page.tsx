import { SavedListingsClient } from "./_client";

export const metadata = {
  title: "Saved listings — LivingSyria",
};

export default function SavedListingsPage() {
  return <SavedListingsClient />;
}
